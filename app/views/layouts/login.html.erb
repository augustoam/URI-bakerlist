<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Probus Server</title>
    <!-- Main css styles -->
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <!-- Main javascript files -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= javascript_tag '$.fx.off = true;' if Rails.env.test? %>
  </head>
  <body class="gray-bg">
    <div>
      <h3 align="center" class="logo-name">P</h3>
    </div>
    <div class="middle-box text-center loginscreen animated fadeInDown">
      <% if notice.present? %>
        <div class="panel panel-primary">
          <div class="panel-heading">Notificação</div>
          <div class="panel-body"><%= notice %></div>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="panel panel-danger">
          <div class="panel-heading">Alerta</div>
          <div class="panel-body"><%= alert %></div>
        </div>
      <% end %>
      <%= yield %>
      <p class="m-t">
        <small>
          <%= link_to "Wonder", "http://www.wonder.com.br", target: "_blank" %> &copy; <%= Date.today.year %>
        </small>
      </p>
    </div>
  </body>
</html>
